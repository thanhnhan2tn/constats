$(document).ready(function() {
	$(".list-server").ready(function() {
		
		$.ajax({
			url:'getservers/'+cc,
			type:'GET',
			data:{},
			timeout:30000,
			success:function(data, status){
				
				console.log(data);
			}
		})
	});
});